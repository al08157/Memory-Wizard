# Make a notification Algorithm 
        # from final import TaskScreen
        # taskscreen = TaskScreen()
        # coordinates = taskscreen.coordinates
        # if coordinates:
        #     coordinates = list(coordinates)
        #     for i in range(len(coordinates)):
        #         x,y = coordinates[i] 
        #         for j in range(len(markets)):
        #             if x == markets[j][1] and y == markets[j][2]:
        #                 print('found it')